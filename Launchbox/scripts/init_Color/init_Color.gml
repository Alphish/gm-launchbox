launchbox_put_instance(obj_TestText, {
    x: 16,
    y: 160,
    text: "The order of executing GML scripts at the start is undetermined\nso use different Launchbox boxes if you need initialisation ordering between them.", color: c_orange
});
